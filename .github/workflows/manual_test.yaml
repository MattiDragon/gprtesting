name: Testing

on:
  workflow_dispatch:
    inputs:
      test_input:
        description: "Test input"
        required: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "${{ format('{0}', inputs.test_input) }}"
